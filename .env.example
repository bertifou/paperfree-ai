# --- Stockage ---
UPLOAD_DIR=./storage/uploads
WATCH_DIR=./storage/watch
DB_DIR=./storage

# --- LLM (compatible OpenAI API) ---
# LM Studio : http://localhost:1234/v1
# Ollama    : http://localhost:11434/v1
# OpenAI    : https://api.openai.com/v1
LLM_BASE_URL=http://localhost:1234/v1
LLM_API_KEY=lm-studio
LLM_MODEL=local-model

# --- OCR & Vision ---
# Correction automatique des erreurs OCR par le LLM (recommandé)
OCR_LLM_CORRECTION=true
# Seuil de confiance Tesseract (%) sous lequel la correction est forcée (0-100)
OCR_CORRECTION_THRESHOLD=80

# Vision multimodale : analyse l'image directement sans OCR
# Désactivée par défaut. Provider : local | openai | anthropic
LLM_VISION_ENABLED=false
LLM_VISION_PROVIDER=local
# Modèle vision (vide = utilise LLM_MODEL). Exemples : gpt-4o, llava-v1.6, claude-3-5-sonnet-20241022
LLM_VISION_MODEL=
# Clé et URL si le provider vision est différent du LLM principal
LLM_VISION_API_KEY=
LLM_VISION_BASE_URL=

# Clés API externes optionnelles (utilisées si vision_provider = openai ou anthropic)
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...

# --- Serveur ---
SECRET_KEY=changeme-please-generate-a-random-string
